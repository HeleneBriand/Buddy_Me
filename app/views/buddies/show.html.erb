<h1>hi from Buddies#show</h1>

<%# <%= simple_form_for [@buddy, @event] do |f| %>
  <%# <%= f.button :submit %>
<%# <% end %>

<%= link_to "Create a reservation for #{@buddy.user.name}", buddy_create_reservation_path(@buddy), data: {turbo_method: :post} %>
